[common]
 log_dir = "<LOGPATH>/logs"
 data_dir = "<LOGPATH>/data"
 debug = false 
 host_prefix = "https://kilonova.ro/"

[database]
 dbtype = "sqlite"
 dsn = "database.db"

[eval]
 isolatePath = "/tmp/isolate"
 compilePath = "/tmp/kncompiles"
 address = "localhost:8001"
 num_concurrent = 3

[email]
 host = "HOST"
 username = "USERNAME"
 password = "PASSWORD"

[index]
 lists_to_show = []
 show_problems = true
 description = "I haven't yet configured KiloNova, the next-gen programming platform that I am using behind this website"
