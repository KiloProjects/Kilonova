import {h, Fragment, Component, render} from 'preact';
import register from 'preact-custom-element';
import {getText} from '../translation';

class ContestPanel extends Component {
	render() {
		return (
			<></>
		)
	}
}

register(ContestPanel, 'kn-contest-panel', [])
